-- @generated by `buddy migrate:regenerate` — edits will be overwritten
CREATE TABLE IF NOT EXISTS "campaign_experiments" (
  "id" BIGSERIAL PRIMARY KEY,
  "kind" "campaign_experiments_kind_type" not null,
  "allocation" jsonb not null,
  "winning_metric" "campaign_experiments_winning_metric_type" not null,
  "status" "campaign_experiments_status_type" not null default 'draft',
  "winner_variant_id" integer,
  "evaluate_at" timestamp,
  "completed_at" timestamp,
  "team_id" bigint REFERENCES "teams"("id"),
  "campaign_id" bigint REFERENCES "campaigns"("id"),
  "created_at" timestamp not null default CURRENT_TIMESTAMP,
  "updated_at" timestamp,
  "uuid" varchar(255)
);
CREATE INDEX IF NOT EXISTS "campaign_experiments_team_campaign_index" ON "campaign_experiments" ("team_id", "campaign_id");
CREATE UNIQUE INDEX IF NOT EXISTS "campaign_experiments_uuid_unique" ON "campaign_experiments" ("uuid");
