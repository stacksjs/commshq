-- @generated by `buddy migrate:regenerate` — edits will be overwritten
CREATE TABLE IF NOT EXISTS "webhook_endpoints" (
  "id" INTEGER PRIMARY KEY AUTOINCREMENT,
  "name" TEXT not null,
  "provider" TEXT CHECK ("provider" IN ('twilio', 'stripe', 'shopify', 'woocommerce', 'mail', 'generic')) not null,
  "direction" TEXT CHECK ("direction" IN ('inbound', 'outbound')) not null default 'inbound',
  "url" TEXT,
  "encrypted_secret" TEXT not null,
  "events" TEXT not null,
  "status" TEXT CHECK ("status" IN ('active', 'paused', 'failing')) not null default 'active',
  "team_id" INTEGER,
  "created_at" TEXT not null default CURRENT_TIMESTAMP,
  "updated_at" TEXT,
  "uuid" TEXT
);
CREATE UNIQUE INDEX IF NOT EXISTS "webhook_endpoints_team_name_unique" ON "webhook_endpoints" ("team_id", "provider", "name");
CREATE UNIQUE INDEX IF NOT EXISTS "webhook_endpoints_uuid_unique" ON "webhook_endpoints" ("uuid");
