-- @generated by `buddy migrate:regenerate` — edits will be overwritten
CREATE TABLE IF NOT EXISTS "ad_inventory" (
  "id" INTEGER PRIMARY KEY AUTOINCREMENT,
  "name" TEXT not null,
  "format" TEXT CHECK ("format" IN ('newsletter', 'web', 'podcast', 'recommendation')) not null,
  "position" TEXT not null,
  "rate" INTEGER not null,
  "currency" TEXT not null default 'usd',
  "capacity" INTEGER not null default 1,
  "status" TEXT CHECK ("status" IN ('active', 'paused', 'sold_out', 'archived')) not null default 'active',
  "team_id" INTEGER REFERENCES "teams"("id"),
  "publication_id" INTEGER REFERENCES "publications"("id"),
  "created_at" TEXT not null default CURRENT_TIMESTAMP,
  "updated_at" TEXT,
  "uuid" TEXT
);
CREATE INDEX IF NOT EXISTS "ad_inventory_team_publication_index" ON "ad_inventory" ("team_id", "publication_id", "status");
CREATE UNIQUE INDEX IF NOT EXISTS "ad_inventory_uuid_unique" ON "ad_inventory" ("uuid");
