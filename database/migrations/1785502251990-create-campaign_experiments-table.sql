-- @generated by `buddy migrate:regenerate` — edits will be overwritten
CREATE TABLE IF NOT EXISTS "campaign_experiments" (
  "id" INTEGER PRIMARY KEY AUTOINCREMENT,
  "kind" TEXT CHECK ("kind" IN ('subject', 'sender', 'content', 'send_time')) not null,
  "allocation" TEXT not null,
  "winning_metric" TEXT CHECK ("winning_metric" IN ('open_rate', 'click_rate', 'conversion_rate', 'revenue')) not null,
  "status" TEXT CHECK ("status" IN ('draft', 'running', 'evaluating', 'completed', 'cancelled')) not null default 'draft',
  "winner_variant_id" INTEGER,
  "evaluate_at" TEXT,
  "completed_at" TEXT,
  "team_id" INTEGER REFERENCES "teams"("id"),
  "campaign_id" INTEGER REFERENCES "campaigns"("id"),
  "created_at" TEXT not null default CURRENT_TIMESTAMP,
  "updated_at" TEXT,
  "uuid" TEXT
);
CREATE INDEX IF NOT EXISTS "campaign_experiments_team_campaign_index" ON "campaign_experiments" ("team_id", "campaign_id");
CREATE UNIQUE INDEX IF NOT EXISTS "campaign_experiments_uuid_unique" ON "campaign_experiments" ("uuid");
